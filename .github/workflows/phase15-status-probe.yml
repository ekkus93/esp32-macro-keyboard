name: Phase 15 status probe

on:
  push:
    branches:
      - master
    paths:
      - ".github/workflows/phase15-status-probe.yml"

permissions:
  actions: read
  contents: write

jobs:
  probe:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Query completion run
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          curl --fail --location \
            --header "Accept: application/vnd.github+json" \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/workflows/complete-phase15-once.yml/runs?per_page=3" \
            --output /tmp/runs.json
          mkdir -p docs
          {
            echo "# Phase 15 run status"
            echo
            echo '```json'
            jq '{runs: [.workflow_runs[] | {id, head_sha, status, conclusion, html_url}]}' /tmp/runs.json
            echo '```'
          } > docs/CI_PHASE15_RUN_STATUS.md
      - name: Commit status
        run: |
          set -euo pipefail
          git config user.name "ChatGPT"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/CI_PHASE15_RUN_STATUS.md
          git commit -m "ci: update Phase 15 run status"
          git fetch origin master
          git rebase origin/master
          git push origin HEAD:master

# Probe generation 5
