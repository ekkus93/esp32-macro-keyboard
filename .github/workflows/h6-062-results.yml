name: H6-062 Async Result Validator

on:
  push:
    branches:
      - master
    paths:
      - ".github/workflows/h6-062-results.yml"

permissions:
  contents: write

jobs:
  validate-and-record:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out staging commit
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Install host-test dependencies
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends build-essential clang-format cmake libcjson-dev

      - name: Add H6-062 worker-capable regression harness
        shell: bash
        run: |
          set -euo pipefail
          cat > tests/host/test_web_server_async_results.c <<'EOF'
          /* Worker-capable host regression for H6-062.
           *
           * Unlike test_web_server_async_confirmation.c's deliberate dead-path
           * FreeRTOS canaries, this target supplies a small pthread-backed queue,
           * semaphore, task, and critical-section model so the real
           * web_server_async.c worker actually runs. The model is intentionally
           * limited to the one-slot queue/binary semaphore semantics this module
           * uses; it is not a general FreeRTOS emulator.
           *
           * The tests prove that handler/send failures never skip async request
           * completion, completion failures are observed in HTTP health, queue
           * failure completes the already-cloned request and releases in-flight
           * ownership, and a failed stop signal is returned/health-recorded while
           * leaving the worker state retryable for a later clean stop. */

          #include <pthread.h>
          #include <stdbool.h>
          #include <stddef.h>
          #include <stdint.h>
          #include <stdio.h>
          #include <stdlib.h>
          #include <string.h>

          #include "app_error.h"
          #include "esp_http_server.h"
          #include "esp_system.h"
          #include "freertos/FreeRTOS.h"
          #include "freertos/queue.h"
          #include "freertos/semphr.h"
          #include "freertos/task.h"
          #include "http_health.h"
          #include "test_assert.h"
          #include "web_api_core.h"
          #include "web_http_status.h"
          #include "web_server_internal.h"

          /* ------------------------------------------------------------------
           * Focused pthread-backed FreeRTOS subset.
           * ------------------------------------------------------------------ */

          struct QueueDefinition {
              pthread_mutex_t mutex;
              pthread_cond_t changed;
              bool active;
              bool full;
              void *value;
          };

          static struct QueueDefinition g_queue = {
              .mutex = PTHREAD_MUTEX_INITIALIZER,
              .changed = PTHREAD_COND_INITIALIZER,
          };
          static struct QueueDefinition g_semaphore = {
              .mutex = PTHREAD_MUTEX_INITIALIZER,
              .changed = PTHREAD_COND_INITIALIZER,
          };
          static bool g_fail_next_queue_send;

          struct tskTaskControlBlock {
              pthread_t thread;
          };

          static struct tskTaskControlBlock g_task;
          static pthread_mutex_t g_task_mutex = PTHREAD_MUTEX_INITIALIZER;
          static pthread_cond_t g_task_changed = PTHREAD_COND_INITIALIZER;
          static bool g_task_running;
          static TaskFunction_t g_task_function;
          static void *g_task_context;

          static pthread_mutex_t g_port_lock = PTHREAD_MUTEX_INITIALIZER;

          static void fake_freertos_wait_for_idle(void) {
              TEST_CHECK_EQ_INT(0, pthread_mutex_lock(&g_task_mutex));
              while (g_task_running) {
                  TEST_CHECK_EQ_INT(0, pthread_cond_wait(&g_task_changed, &g_task_mutex));
              }
              TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_task_mutex));
          }

          static void fake_freertos_fail_next_queue_send(void) {
              TEST_CHECK_EQ_INT(0, pthread_mutex_lock(&g_queue.mutex));
              g_fail_next_queue_send = true;
              TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_queue.mutex));
          }

          QueueHandle_t xQueueCreate(UBaseType_t queue_length, UBaseType_t item_size) {
              if (queue_length != 1U || item_size != sizeof(void *)) {
                  return NULL;
              }
              TEST_CHECK_EQ_INT(0, pthread_mutex_lock(&g_queue.mutex));
              g_queue.active = true;
              g_queue.full = false;
              g_queue.value = NULL;
              g_fail_next_queue_send = false;
              TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_queue.mutex));
              return &g_queue;
          }

          BaseType_t xQueueSend(QueueHandle_t queue, const void *item, TickType_t ticks_to_wait) {
              if (queue != &g_queue || item == NULL) {
                  return pdFAIL;
              }
              TEST_CHECK_EQ_INT(0, pthread_mutex_lock(&g_queue.mutex));
              if (g_fail_next_queue_send) {
                  g_fail_next_queue_send = false;
                  TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_queue.mutex));
                  return pdFAIL;
              }
              while (g_queue.active && g_queue.full) {
                  if (ticks_to_wait == 0U) {
                      TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_queue.mutex));
                      return pdFAIL;
                  }
                  TEST_CHECK_EQ_INT(0, pthread_cond_wait(&g_queue.changed, &g_queue.mutex));
              }
              if (!g_queue.active) {
                  TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_queue.mutex));
                  return pdFAIL;
              }
              memcpy(&g_queue.value, item, sizeof(g_queue.value));
              g_queue.full = true;
              TEST_CHECK_EQ_INT(0, pthread_cond_broadcast(&g_queue.changed));
              TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_queue.mutex));
              return pdTRUE;
          }

          BaseType_t xQueueReceive(QueueHandle_t queue, void *out_item, TickType_t ticks_to_wait) {
              if (queue != &g_queue || out_item == NULL) {
                  return pdFAIL;
              }
              TEST_CHECK_EQ_INT(0, pthread_mutex_lock(&g_queue.mutex));
              while (g_queue.active && !g_queue.full) {
                  if (ticks_to_wait == 0U) {
                      TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_queue.mutex));
                      return pdFAIL;
                  }
                  TEST_CHECK_EQ_INT(0, pthread_cond_wait(&g_queue.changed, &g_queue.mutex));
              }
              if (!g_queue.active) {
                  TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_queue.mutex));
                  return pdFAIL;
              }
              memcpy(out_item, &g_queue.value, sizeof(g_queue.value));
              g_queue.value = NULL;
              g_queue.full = false;
              TEST_CHECK_EQ_INT(0, pthread_cond_broadcast(&g_queue.changed));
              TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_queue.mutex));
              return pdTRUE;
          }

          void vQueueDelete(QueueHandle_t queue) {
              TEST_CHECK(queue == &g_queue);
              TEST_CHECK_EQ_INT(0, pthread_mutex_lock(&g_queue.mutex));
              g_queue.active = false;
              g_queue.full = false;
              g_queue.value = NULL;
              TEST_CHECK_EQ_INT(0, pthread_cond_broadcast(&g_queue.changed));
              TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_queue.mutex));
          }

          SemaphoreHandle_t xSemaphoreCreateBinary(void) {
              TEST_CHECK_EQ_INT(0, pthread_mutex_lock(&g_semaphore.mutex));
              g_semaphore.active = true;
              g_semaphore.full = false;
              TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_semaphore.mutex));
              return &g_semaphore;
          }

          BaseType_t xSemaphoreGive(SemaphoreHandle_t semaphore) {
              if (semaphore != &g_semaphore) {
                  return pdFAIL;
              }
              TEST_CHECK_EQ_INT(0, pthread_mutex_lock(&g_semaphore.mutex));
              if (!g_semaphore.active) {
                  TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_semaphore.mutex));
                  return pdFAIL;
              }
              g_semaphore.full = true;
              TEST_CHECK_EQ_INT(0, pthread_cond_broadcast(&g_semaphore.changed));
              TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_semaphore.mutex));
              return pdTRUE;
          }

          BaseType_t xSemaphoreTake(SemaphoreHandle_t semaphore, TickType_t ticks_to_wait) {
              if (semaphore != &g_semaphore) {
                  return pdFAIL;
              }
              TEST_CHECK_EQ_INT(0, pthread_mutex_lock(&g_semaphore.mutex));
              while (g_semaphore.active && !g_semaphore.full) {
                  if (ticks_to_wait == 0U) {
                      TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_semaphore.mutex));
                      return pdFAIL;
                  }
                  TEST_CHECK_EQ_INT(0,
                                    pthread_cond_wait(&g_semaphore.changed, &g_semaphore.mutex));
              }
              if (!g_semaphore.active) {
                  TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_semaphore.mutex));
                  return pdFAIL;
              }
              g_semaphore.full = false;
              TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_semaphore.mutex));
              return pdTRUE;
          }

          void vSemaphoreDelete(SemaphoreHandle_t semaphore) {
              TEST_CHECK(semaphore == &g_semaphore);
              TEST_CHECK_EQ_INT(0, pthread_mutex_lock(&g_semaphore.mutex));
              g_semaphore.active = false;
              g_semaphore.full = false;
              TEST_CHECK_EQ_INT(0, pthread_cond_broadcast(&g_semaphore.changed));
              TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_semaphore.mutex));
          }

          static void *fake_task_entry(void *unused) {
              (void)unused;
              g_task_function(g_task_context);
              TEST_CHECK_EQ_INT(0, pthread_mutex_lock(&g_task_mutex));
              g_task_running = false;
              TEST_CHECK_EQ_INT(0, pthread_cond_broadcast(&g_task_changed));
              TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_task_mutex));
              return NULL;
          }

          BaseType_t xTaskCreate(TaskFunction_t task_code, const char *name, uint32_t stack_depth,
                                 void *parameters, UBaseType_t priority, TaskHandle_t *out_handle) {
              (void)name;
              (void)stack_depth;
              (void)priority;
              if (task_code == NULL || out_handle == NULL) {
                  return pdFAIL;
              }
              TEST_CHECK_EQ_INT(0, pthread_mutex_lock(&g_task_mutex));
              if (g_task_running) {
                  TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_task_mutex));
                  return pdFAIL;
              }
              g_task_function = task_code;
              g_task_context = parameters;
              g_task_running = true;
              const int create_result = pthread_create(&g_task.thread, NULL, fake_task_entry, NULL);
              if (create_result != 0) {
                  g_task_running = false;
                  TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_task_mutex));
                  return pdFAIL;
              }
              TEST_CHECK_EQ_INT(0, pthread_detach(g_task.thread));
              *out_handle = &g_task;
              TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_task_mutex));
              return pdPASS;
          }

          void vTaskDelete(TaskHandle_t task) {
              TEST_CHECK(task == NULL);
          }

          void vPortEnterCritical(portMUX_TYPE *mux) {
              (void)mux;
              TEST_CHECK_EQ_INT(0, pthread_mutex_lock(&g_port_lock));
          }

          void vPortExitCritical(portMUX_TYPE *mux) {
              (void)mux;
              TEST_CHECK_EQ_INT(0, pthread_mutex_unlock(&g_port_lock));
          }

          /* ------------------------------------------------------------------
           * HTTP/API seams around the real worker.
           * ------------------------------------------------------------------ */

          static esp_err_t g_handler_result;
          static bool g_handler_should_restart;
          static size_t g_handler_calls;
          static esp_err_t g_completion_result;
          static size_t g_completion_calls;
          static size_t g_begin_calls;
          static esp_err_t g_status_send_result;
          static size_t g_status_send_calls;
          static unsigned int g_last_status;
          static app_error_code_t g_last_status_code;
          static size_t g_restart_calls;
          static httpd_req_t g_async_request;

          app_error_code_t web_api_read_route_body(httpd_req_t *request, char **out_body,
                                                   size_t *out_length) {
              (void)request;
              *out_body = NULL;
              *out_length = 0U;
              return APP_ERROR_NOT_FOUND;
          }

          esp_err_t web_api_handle_call_with_body(httpd_req_t *request, char *preread_body,
                                                  size_t preread_length,
                                                  bool *out_should_restart) {
              (void)request;
              (void)preread_length;
              ++g_handler_calls;
              free(preread_body);
              *out_should_restart = g_handler_should_restart;
              return g_handler_result;
          }

          esp_err_t web_api_send_status_error(httpd_req_t *request, unsigned int status,
                                              app_error_code_t code, const char *message) {
              (void)request;
              (void)message;
              ++g_status_send_calls;
              g_last_status = status;
              g_last_status_code = code;
              return g_status_send_result;
          }

          unsigned int web_api_http_status_for_error(app_error_code_t error) {
              (void)error;
              return 500U;
          }

          esp_err_t httpd_req_async_handler_begin(httpd_req_t *request, httpd_req_t **out_request) {
              ++g_begin_calls;
              g_async_request = *request;
              *out_request = &g_async_request;
              return ESP_OK;
          }

          esp_err_t httpd_req_async_handler_complete(httpd_req_t *request) {
              TEST_CHECK(request == &g_async_request);
              ++g_completion_calls;
              return g_completion_result;
          }

          void esp_restart(void) {
              ++g_restart_calls;
          }

          static void reset_fakes(void) {
              fake_freertos_wait_for_idle();
              g_handler_result = ESP_OK;
              g_handler_should_restart = false;
              g_handler_calls = 0U;
              g_completion_result = ESP_OK;
              g_completion_calls = 0U;
              g_begin_calls = 0U;
              g_status_send_result = ESP_OK;
              g_status_send_calls = 0U;
              g_last_status = 0U;
              g_last_status_code = APP_ERROR_NONE;
              g_restart_calls = 0U;
              http_health_reset();
          }

          static httpd_req_t request_fixture(void) {
              httpd_req_t request = {0};
              request.method = HTTP_POST;
              memcpy(request.uri, "/api/v1/device/restart", sizeof("/api/v1/device/restart"));
              return request;
          }

          static void assert_async_health(http_async_failure_stage_t stage,
                                          app_error_code_t error) {
              const http_health_t health = http_health_snapshot();
              TEST_CHECK_EQ_INT(SUBSYSTEM_HEALTH_FAILED, health.state);
              TEST_CHECK_EQ_INT(stage, health.async_failure_stage);
              TEST_CHECK_APP_ERROR(error, health.async_error);
          }

          static void test_handler_failure_still_completes_request(void) {
              reset_fakes();
              g_handler_result = ESP_FAIL;
              TEST_CHECK_APP_ERROR(APP_ERROR_NONE, web_server_async_start());
              httpd_req_t request = request_fixture();

              TEST_CHECK_EQ_INT(ESP_OK, web_server_async_dispatch(&request));
              TEST_CHECK_APP_ERROR(APP_ERROR_NONE, web_server_async_stop());
              fake_freertos_wait_for_idle();

              TEST_CHECK_EQ_U64(1U, (uint64_t)g_handler_calls);
              TEST_CHECK_EQ_U64(1U, (uint64_t)g_completion_calls);
              TEST_CHECK_EQ_U64(0U, (uint64_t)g_restart_calls);
              assert_async_health(HTTP_ASYNC_FAILURE_WORKER_RUN, APP_ERROR_IO);
          }

          static void test_completion_failure_is_observed_and_stop_remains_safe(void) {
              reset_fakes();
              g_completion_result = ESP_FAIL;
              TEST_CHECK_APP_ERROR(APP_ERROR_NONE, web_server_async_start());
              httpd_req_t request = request_fixture();

              TEST_CHECK_EQ_INT(ESP_OK, web_server_async_dispatch(&request));
              TEST_CHECK_APP_ERROR(APP_ERROR_NONE, web_server_async_stop());
              fake_freertos_wait_for_idle();

              TEST_CHECK_EQ_U64(1U, (uint64_t)g_handler_calls);
              TEST_CHECK_EQ_U64(1U, (uint64_t)g_completion_calls);
              assert_async_health(HTTP_ASYNC_FAILURE_COMPLETION, APP_ERROR_IO);
          }

          static void test_queue_failure_completes_clone_and_releases_in_flight(void) {
              reset_fakes();
              TEST_CHECK_APP_ERROR(APP_ERROR_NONE, web_server_async_start());
              httpd_req_t first = request_fixture();
              fake_freertos_fail_next_queue_send();

              TEST_CHECK_EQ_INT(ESP_OK, web_server_async_dispatch(&first));
              TEST_CHECK_EQ_U64(1U, (uint64_t)g_begin_calls);
              TEST_CHECK_EQ_U64(1U, (uint64_t)g_status_send_calls);
              TEST_CHECK_EQ_U64(WEB_HTTP_STATUS_SERVICE_UNAVAILABLE, (uint64_t)g_last_status);
              TEST_CHECK_APP_ERROR(APP_ERROR_INTERNAL, g_last_status_code);
              TEST_CHECK_EQ_U64(1U, (uint64_t)g_completion_calls);
              TEST_CHECK_EQ_U64(0U, (uint64_t)g_handler_calls);
              assert_async_health(HTTP_ASYNC_FAILURE_QUEUE, APP_ERROR_INTERNAL);

              /* The failed queue attempt must relinquish in-flight ownership.
               * A second request can enter the real worker instead of being
               * rejected as a phantom pending confirmation. */
              httpd_req_t second = request_fixture();
              TEST_CHECK_EQ_INT(ESP_OK, web_server_async_dispatch(&second));
              TEST_CHECK_APP_ERROR(APP_ERROR_NONE, web_server_async_stop());
              fake_freertos_wait_for_idle();
              TEST_CHECK_EQ_U64(2U, (uint64_t)g_begin_calls);
              TEST_CHECK_EQ_U64(1U, (uint64_t)g_handler_calls);
              TEST_CHECK_EQ_U64(2U, (uint64_t)g_completion_calls);
          }

          static void test_stop_signal_failure_is_returned_and_retryable(void) {
              reset_fakes();
              TEST_CHECK_APP_ERROR(APP_ERROR_NONE, web_server_async_start());
              fake_freertos_fail_next_queue_send();

              TEST_CHECK_APP_ERROR(APP_ERROR_TIMEOUT, web_server_async_stop());
              assert_async_health(HTTP_ASYNC_FAILURE_WORKER_STOP, APP_ERROR_TIMEOUT);

              /* Stop failure must not destroy queue/semaphore/task ownership.
               * Retrying the stop sends a fresh sentinel and completes cleanup. */
              TEST_CHECK_APP_ERROR(APP_ERROR_NONE, web_server_async_stop());
              fake_freertos_wait_for_idle();
          }

          int main(void) {
              test_handler_failure_still_completes_request();
              test_completion_failure_is_observed_and_stop_remains_safe();
              test_queue_failure_completes_clone_and_releases_in_flight();
              test_stop_signal_failure_is_returned_and_retryable();
              puts("web server async result/cleanup tests passed");
              return 0;
          }
          EOF

          python3 - <<'PY'
          from pathlib import Path

          cmake = Path("tests/host/CMakeLists.txt")
          text = cmake.read_text()
          marker = "register_host_test(web_server_async_results_tests web_server_async_results web)"
          if marker not in text:
              text += r'''

          # Worker-capable focused regression for H6-062. Unlike
          # web_server_async_confirmation_tests' dead-path canaries, this target supplies a
          # pthread-backed one-slot queue/binary semaphore/task model directly in the test so the
          # real async worker executes and its handler/completion/stop results can be fault-injected.
          add_executable(
              web_server_async_results_tests
              test_web_server_async_results.c
              ../../firmware/components/web_server/web_server_async.c
              ../../firmware/components/web_server/http_health.c
              ../../firmware/components/support/subsystem_health.c
          )
          target_include_directories(
              web_server_async_results_tests
              PRIVATE fakes/esp_http_server_stub
                      fakes/esp_idf_misc_stub
                      fakes/freertos_stub
                      fakes
                      ../../firmware/components/web_server
                      ../../firmware/components/web_server/include
                      ../../firmware/components/macro_model/include
                      ../../firmware/components/macro_parser/include
                      ../../firmware/components/macro_executor/include
                      ../../firmware/components/app_contracts_v2/include
                      ../../firmware/components/auth/include
                      ../../firmware/components/device_controls/include
                      ../../firmware/components/storage/include
                      ../../firmware/components/wifi_ap/include
                      ../../firmware/components/usb_keyboard/include
                      ../../firmware/components/support/include
          )
          target_link_libraries(web_server_async_results_tests PRIVATE Threads::Threads)
          register_host_test(web_server_async_results_tests web_server_async_results web)
          '''.lstrip("\n")
              cmake.write_text(text)

          header = Path("tests/host/fakes/esp_http_server_stub/esp_http_server.h")
          text = header.read_text()
          old = '''/* Only web_server_async.c calls these two (httpd_req_async_handler_begin()'s
           * "hand the request to a worker task" branch of web_server_async_dispatch());
           * signatures match the real ESP-IDF esp_http_server.h exactly. Declared here
           * (alongside the httpd_start()/httpd_register_uri_handler() route-registration
           * subset below, which follows the same "declared for every target, defined
           * only where actually exercised" pattern) but never given a working
           * definition anywhere: every target that links web_server_async.c
           * deliberately never reaches the branch that calls these -- see
           * fakes/freertos_stub/freertos/FreeRTOS.h's header comment and
           * test_web_server_async_confirmation.c for why, and what each one's
           * dead-path canary definition looks like there. */'''
          new = '''/* Only web_server_async.c calls these two (httpd_req_async_handler_begin()'s
           * "hand the request to a worker task" branch of web_server_async_dispatch());
           * signatures match the real ESP-IDF esp_http_server.h exactly. The H6-060
           * web_server_async_confirmation target defines hard-failure canaries because its worker
           * must remain unavailable; the H6-062 web_server_async_results target defines a focused
           * working clone/complete seam so the real worker cleanup path can be fault-injected. */'''
          if text.count(old) != 1:
              raise SystemExit("expected async-httpd stub comment block")
          header.write_text(text.replace(old, new, 1))
          PY

          clang-format -i tests/host/test_web_server_async_results.c tests/host/fakes/esp_http_server_stub/esp_http_server.h
          git diff --check

      - name: Run web host tests
        shell: bash
        run: ./scripts/run-tests.sh web

      - name: Run web host tests with ASan and UBSan
        shell: bash
        run: ./scripts/run-tests.sh --sanitizers web

      - name: Commit validated H6-062 regression proof
        shell: bash
        run: |
          set -euo pipefail
          git add tests/host/CMakeLists.txt tests/host/test_web_server_async_results.c tests/host/fakes/esp_http_server_stub/esp_http_server.h
          git diff --cached --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "test(web): prove async completion cleanup"
          git rev-parse HEAD > "$RUNNER_TEMP/h6-062-product-sha"
          git push origin HEAD:master

      - name: Record H6-062 evidence and ledger
        shell: bash
        run: |
          set -euo pipefail
          product_sha="$(cat "$RUNNER_TEMP/h6-062-product-sha")"
          cat > docs/implementation-v2/H6_062_ASYNC_COMPLETION_RESULTS_2026-08-11.md <<EOF
          # H6-062 — Async completion-result and cleanup evidence

          **Date:** 2026-08-11
          **Task:** \`H6-062 — Stop ignoring completion results\`
          **Implementation/test SHA:** \`${product_sha}\`

          ## Result handling

          The runtime result-observation code was introduced in H6-061 at \`8f4ccfae3abf3803f70fc487cb6471039d9d13ab\`: \`web_api_handle_call_with_body()\`, \`httpd_req_async_handler_complete()\`, and the stop-signal \`xQueueSend()\` are all checked and mapped into sanitized async HTTP health. H6-062 adds the worker-capable proof that those observations preserve request/socket cleanup semantics instead of only recording counters on dead paths.

          ## Worker-capable host seam

          \`test_web_server_async_results.c\` links the real \`web_server_async.c\` and supplies a deliberately narrow pthread-backed model for exactly the FreeRTOS primitives it uses: a one-slot queue, binary semaphore, task, and critical section. This is not a general FreeRTOS emulator. It exists so the real worker loop, async clone/completion path, in-flight ownership, and stop sentinel can execute under deterministic fault injection.

          The older \`web_server_async_confirmation\` target remains a deliberate H6-060 dead-path test: its FreeRTOS/httpd async functions are still hard-failure canaries and it never starts the worker.

          ## Cleanup-safety regressions

          The new worker-capable target proves four properties:

          1. A handler/send failure is health-recorded as \`WORKER_RUN\` and still calls \`httpd_req_async_handler_complete()\` exactly once before the worker is stopped.
          2. An async-completion failure is observed as \`COMPLETION\`; the worker still releases in-flight ownership and can complete bounded shutdown.
          3. A dispatch queue-send failure sends the visible 503 path, completes the already-cloned async request, releases in-flight ownership, and permits a subsequent request to enter the worker rather than leaving a phantom pending confirmation.
          4. A stop-signal send failure is returned as \`APP_ERROR_TIMEOUT\` and health-recorded as \`WORKER_STOP\` without destroying queue/semaphore/task ownership; a later stop retry succeeds and performs cleanup.

          ## Validation

          Targeted workflow run **${GITHUB_RUN_ID}**, job **${GITHUB_JOB}**, ran \`./scripts/run-tests.sh web\` and \`./scripts/run-tests.sh --sanitizers web\`. Both suites passed with the new \`web_server_async_results\` test included. The workflow also ran clang-format on the changed C/header files and \`git diff --check\` before committing.

          ## Scope

          This closes H6-062 only. The H6-063 matrix remains open for its explicit pending-confirmation bounded-stop and unrelated-status responsiveness/concurrency cases; the new worker-capable seam is intended to support those next.
          EOF

          python3 - <<'PY'
          from pathlib import Path
          import os

          path = Path("docs/ESP32_MACRO_KEYBOARD_POST_V2_CORRECTNESS_HARDENING_TODO_2026-08-10.md")
          text = path.read_text()
          old = '''### H6-062 — Stop ignoring completion results

          - [ ] Observe `web_api_handle_call_with_body` result.
          - [ ] Observe `httpd_req_async_handler_complete` result.
          - [ ] Observe stop-signal result.
          - [ ] Preserve socket/request cleanup safety even when an error cannot be returned to the original client.
          '''.replace("          ", "")
          product_sha = Path(os.environ["RUNNER_TEMP"]).joinpath("h6-062-product-sha").read_text().strip()
          run_id = os.environ["GITHUB_RUN_ID"]
          new = f'''### H6-062 — Stop ignoring completion results

          - [x] Observe `web_api_handle_call_with_body` result.
          - [x] Observe `httpd_req_async_handler_complete` result.
          - [x] Observe stop-signal result.
          - [x] Preserve socket/request cleanup safety even when an error cannot be returned to the original client.
          - Evidence: runtime observation landed with H6-061 commit `8f4ccfae3abf3803f70fc487cb6471039d9d13ab`; worker-capable cleanup proof `{product_sha}` executes the real async worker under handler, completion, queue-send, and stop-signal faults. `./scripts/run-tests.sh web` and `./scripts/run-tests.sh --sanitizers web` passed in targeted run `{run_id}`. Full evidence: `docs/implementation-v2/H6_062_ASYNC_COMPLETION_RESULTS_2026-08-11.md`.
          '''.replace("          ", "")
          if text.count(old) != 1:
              raise SystemExit("expected exactly one H6-062 ledger block")
          path.write_text(text.replace(old, new, 1))
          PY

          git rm .github/workflows/h6-062-results.yml
          git add docs/ESP32_MACRO_KEYBOARD_POST_V2_CORRECTNESS_HARDENING_TODO_2026-08-10.md docs/implementation-v2/H6_062_ASYNC_COMPLETION_RESULTS_2026-08-11.md
          git diff --cached --check
          git commit -m "docs: record H6-062 async completion evidence"
          git push origin HEAD:master
