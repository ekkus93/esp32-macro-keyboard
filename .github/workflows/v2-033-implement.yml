name: V2-033 Implementation

on:
  push:
    branches:
      - master
    paths:
      - .github/workflows/v2-033-implement.yml

permissions:
  contents: write

jobs:
  implement:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install host dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends build-essential cmake libcjson-dev clang-format

      - name: Implement V2-033
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path

          def replace_once(path: str, old: str, new: str) -> None:
              file = Path(path)
              text = file.read_text()
              if old not in text:
                  raise SystemExit(f"expected text not found in {path}: {old[:100]!r}")
              file.write_text(text.replace(old, new, 1))

          replace_once(
              "firmware/components/storage/include/storage_blob.h",
              "#define STORAGE_BLOB_FILENAME_CAPACITY (STORAGE_BLOB_FILENAME_LENGTH + 1U)\n",
              "#define STORAGE_BLOB_FILENAME_CAPACITY (STORAGE_BLOB_FILENAME_LENGTH + 1U)\n"
              "#define STORAGE_BLOB_TEMP_FILENAME_LENGTH (STORAGE_BLOB_ID_DIGITS + 7U)\n"
              "#define STORAGE_BLOB_TEMP_FILENAME_CAPACITY (STORAGE_BLOB_TEMP_FILENAME_LENGTH + 1U)\n",
          )
          replace_once(
              "firmware/components/storage/include/storage_blob.h",
              "    size_t invalid_name_count;\n",
              "    size_t invalid_name_count;\n    size_t temporary_file_count;\n",
          )
          replace_once(
              "firmware/components/storage/include/storage_blob.h",
              "typedef struct {\n    storage_blob_scan_summary_t summary;\n    size_t reported_invalid_name_count;\n    bool invalid_names_truncated;\n    char invalid_names[STORAGE_BLOB_DIAGNOSTIC_INVALID_NAME_MAX]\n                      [STORAGE_BLOB_DIAGNOSTIC_NAME_CAPACITY];\n} storage_blob_diagnostics_t;\n",
              "typedef struct {\n    size_t removed_temporary_file_count;\n} storage_blob_recovery_summary_t;\n\n"
              "typedef struct {\n    storage_blob_scan_summary_t summary;\n    size_t reported_invalid_name_count;\n    bool invalid_names_truncated;\n    char invalid_names[STORAGE_BLOB_DIAGNOSTIC_INVALID_NAME_MAX]\n                      [STORAGE_BLOB_DIAGNOSTIC_NAME_CAPACITY];\n    size_t reported_temporary_file_count;\n    bool temporary_files_truncated;\n    char temporary_files[STORAGE_BLOB_DIAGNOSTIC_INVALID_NAME_MAX]\n                        [STORAGE_BLOB_DIAGNOSTIC_NAME_CAPACITY];\n} storage_blob_diagnostics_t;\n",
          )
          replace_once(
              "firmware/components/storage/include/storage_blob.h",
              "typedef app_error_code_t (*storage_blob_invalid_name_visitor_t)(void *context, const char *name);\n",
              "typedef app_error_code_t (*storage_blob_invalid_name_visitor_t)(void *context, const char *name);\n"
              "typedef app_error_code_t (*storage_blob_temporary_file_visitor_t)(void *context,\n"
              "                                                                 const char *name);\n",
          )
          replace_once(
              "firmware/components/storage/include/storage_blob.h",
              "    storage_blob_invalid_name_visitor_t visit_invalid_name;\n",
              "    storage_blob_invalid_name_visitor_t visit_invalid_name;\n"
              "    storage_blob_temporary_file_visitor_t visit_temporary_file;\n",
          )
          replace_once(
              "firmware/components/storage/include/storage_blob.h",
              "bool storage_blob_parse_filename(const char *name, uint64_t *out_id);\n",
              "bool storage_blob_parse_filename(const char *name, uint64_t *out_id);\n"
              "bool storage_blob_parse_temporary_filename(const char *name, uint64_t *out_id);\n",
          )
          replace_once(
              "firmware/components/storage/include/storage_blob.h",
              "app_error_code_t storage_blob_scan_startup(uint64_t persisted_next_id);\n",
              "app_error_code_t storage_blob_recover_startup(void);\n"
              "app_error_code_t storage_blob_scan_startup(uint64_t persisted_next_id);\n",
          )

          replace_once(
              "firmware/components/storage/storage_blob_internal.h",
              "app_error_code_t storage_blob_scan_with_ops(const storage_fs_ops_t *operations,\n",
              "app_error_code_t storage_blob_recover_with_ops(\n"
              "    const storage_fs_ops_t *operations, const char *directory_path,\n"
              "    storage_blob_recovery_summary_t *out_summary);\n"
              "app_error_code_t storage_blob_scan_with_ops(const storage_fs_ops_t *operations,\n",
          )

          Path("firmware/components/storage/storage_blob_recovery.c").write_text(r'''#include "storage_blob_internal.h"

#include <errno.h>
#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdio.h>
#include <string.h>
#include <sys/stat.h>

#include "app_error.h"
#include "storage_blob.h"
#include "storage_fs_ops.h"

static bool is_dot_entry(const char *name) {
    return strcmp(name, ".") == 0 || strcmp(name, "..") == 0;
}

bool storage_blob_parse_temporary_filename(const char *name, uint64_t *out_id) {
    if (out_id != NULL) {
        *out_id = 0U;
    }
    if (name == NULL || out_id == NULL || strlen(name) != STORAGE_BLOB_TEMP_FILENAME_LENGTH ||
        strcmp(name + STORAGE_BLOB_ID_DIGITS, ".gz.tmp") != 0) {
        return false;
    }

    uint64_t value = 0U;
    for (size_t index = 0U; index < STORAGE_BLOB_ID_DIGITS; ++index) {
        const unsigned char character = (unsigned char)name[index];
        if (character < (unsigned char)'0' || character > (unsigned char)'9') {
            return false;
        }
        const uint64_t digit = (uint64_t)(character - (unsigned char)'0');
        if (value > (UINT64_MAX - digit) / UINT64_C(10)) {
            return false;
        }
        value = (value * UINT64_C(10)) + digit;
    }
    if (value == 0U) {
        return false;
    }
    *out_id = value;
    return true;
}

static app_error_code_t join_path(const char *directory_path, const char *name, char *out_path,
                                  size_t path_size) {
    const int written = snprintf(out_path, path_size, "%s/%s", directory_path, name);
    if (written < 0 || (size_t)written >= path_size) {
        return APP_ERROR_STORAGE_CORRUPT;
    }
    return APP_ERROR_NONE;
}

app_error_code_t storage_blob_recover_with_ops(
    const storage_fs_ops_t *operations, const char *directory_path,
    storage_blob_recovery_summary_t *out_summary) {
    if (!storage_fs_ops_has_directory(operations) || directory_path == NULL ||
        directory_path[0] == '\0' || out_summary == NULL) {
        return APP_ERROR_INVALID_ARGUMENT;
    }
    *out_summary = (storage_blob_recovery_summary_t){0};

    void *directory = operations->open_directory(operations->context, directory_path);
    if (directory == NULL) {
        return APP_ERROR_IO;
    }

    app_error_code_t result = APP_ERROR_NONE;
    while (result == APP_ERROR_NONE) {
        char name[STORAGE_FS_ENTRY_NAME_MAX] = {0};
        bool end = false;
        if (operations->read_directory(operations->context, directory, name, sizeof(name), &end) !=
            0) {
            result = APP_ERROR_IO;
            break;
        }
        if (end) {
            break;
        }
        if (is_dot_entry(name)) {
            continue;
        }

        uint64_t temporary_id = 0U;
        if (!storage_blob_parse_temporary_filename(name, &temporary_id)) {
            continue;
        }

        char path[STORAGE_FS_ENTRY_NAME_MAX * 2U] = {0};
        result = join_path(directory_path, name, path, sizeof(path));
        if (result != APP_ERROR_NONE) {
            break;
        }

        struct stat metadata;
        if (operations->stat_path(operations->context, path, &metadata) != 0) {
            result = APP_ERROR_IO;
            break;
        }
        if (!S_ISREG(metadata.st_mode)) {
            continue;
        }
        if (operations->unlink_path(operations->context, path) != 0) {
            result = APP_ERROR_IO;
            break;
        }
        ++out_summary->removed_temporary_file_count;
    }

    if (operations->close_directory(operations->context, directory) != 0 &&
        result == APP_ERROR_NONE) {
        result = APP_ERROR_IO;
    }
    return result;
}

app_error_code_t storage_blob_recover_startup(void) {
    storage_blob_recovery_summary_t summary = {0};
    const app_error_code_t result = storage_blob_recover_with_ops(
        storage_fs_ops_posix(), STORAGE_BLOB_DIRECTORY, &summary);
    if (result != APP_ERROR_NONE) {
        return result;
    }
    if (summary.removed_temporary_file_count > 0U &&
        storage_fs_sync_parent_path(NULL, STORAGE_BLOB_DIRECTORY "/recovery") != 0) {
        return APP_ERROR_IO;
    }
    return APP_ERROR_NONE;
}
''')

          replace_once(
              "firmware/components/storage/storage_blob_core.c",
              "static app_error_code_t notify_entry(const storage_blob_scan_observer_t *observer,\n",
              "static app_error_code_t notify_temporary_file(\n"
              "    const storage_blob_scan_observer_t *observer, const char *name) {\n"
              "    if (observer == NULL || observer->visit_temporary_file == NULL) {\n"
              "        return APP_ERROR_NONE;\n"
              "    }\n"
              "    return observer->visit_temporary_file(observer->context, name);\n"
              "}\n\n"
              "static app_error_code_t notify_entry(const storage_blob_scan_observer_t *observer,\n",
          )
          replace_once(
              "firmware/components/storage/storage_blob_core.c",
              "    uint64_t blob_id = 0U;\n    if (!storage_blob_parse_filename(name, &blob_id)) {\n        ++context->summary->invalid_name_count;\n        return notify_invalid_name(context->observer, name);\n    }\n\n    char path[STORAGE_FS_ENTRY_NAME_MAX * 2U] = {0};\n",
              "    uint64_t blob_id = 0U;\n"
              "    if (!storage_blob_parse_filename(name, &blob_id)) {\n"
              "        uint64_t temporary_id = 0U;\n"
              "        if (storage_blob_parse_temporary_filename(name, &temporary_id)) {\n"
              "            char temporary_path[STORAGE_FS_ENTRY_NAME_MAX * 2U] = {0};\n"
              "            app_error_code_t temporary_result =\n"
              "                join_path(context->directory_path, name, temporary_path,\n"
              "                          sizeof(temporary_path));\n"
              "            if (temporary_result != APP_ERROR_NONE) {\n"
              "                return temporary_result;\n"
              "            }\n"
              "            struct stat temporary_metadata;\n"
              "            if (context->operations->stat_path(context->operations->context,\n"
              "                                               temporary_path,\n"
              "                                               &temporary_metadata) != 0) {\n"
              "                return APP_ERROR_IO;\n"
              "            }\n"
              "            if (S_ISREG(temporary_metadata.st_mode)) {\n"
              "                ++context->summary->temporary_file_count;\n"
              "                return notify_temporary_file(context->observer, name);\n"
              "            }\n"
              "        }\n"
              "        ++context->summary->invalid_name_count;\n"
              "        return notify_invalid_name(context->observer, name);\n"
              "    }\n\n"
              "    char path[STORAGE_FS_ENTRY_NAME_MAX * 2U] = {0};\n",
          )

          replace_once(
              "firmware/components/storage/storage_blob.c",
              "static app_error_code_t capture_invalid_name(void *context, const char *name) {\n",
              "static app_error_code_t capture_temporary_file(void *context, const char *name) {\n"
              "    storage_blob_diagnostics_t *diagnostics = context;\n"
              "    if (diagnostics == NULL || name == NULL) {\n"
              "        return APP_ERROR_INVALID_ARGUMENT;\n"
              "    }\n"
              "    if (diagnostics->reported_temporary_file_count >=\n"
              "        STORAGE_BLOB_DIAGNOSTIC_INVALID_NAME_MAX) {\n"
              "        diagnostics->temporary_files_truncated = true;\n"
              "        return APP_ERROR_NONE;\n"
              "    }\n"
              "    const size_t length = strlen(name);\n"
              "    if (length >= STORAGE_BLOB_DIAGNOSTIC_NAME_CAPACITY) {\n"
              "        return APP_ERROR_STORAGE_CORRUPT;\n"
              "    }\n"
              "    memcpy(diagnostics->temporary_files[diagnostics->reported_temporary_file_count],\n"
              "           name, length + 1U);\n"
              "    ++diagnostics->reported_temporary_file_count;\n"
              "    return APP_ERROR_NONE;\n"
              "}\n\n"
              "static app_error_code_t capture_invalid_name(void *context, const char *name) {\n",
          )
          replace_once(
              "firmware/components/storage/storage_blob.c",
              "        .visit_invalid_name = capture_invalid_name,\n",
              "        .visit_invalid_name = capture_invalid_name,\n"
              "        .visit_temporary_file = capture_temporary_file,\n",
          )

          replace_once(
              "firmware/components/storage/storage_mount_topology.c",
              "    uint64_t persisted_next_id = 0U;\n",
              "    const app_error_code_t recovery_result = storage_blob_recover_startup();\n"
              "    if (recovery_result != APP_ERROR_NONE) {\n"
              "        return recovery_result;\n"
              "    }\n\n"
              "    uint64_t persisted_next_id = 0U;\n",
          )

          replace_once(
              "firmware/components/storage/CMakeLists.txt",
              '    "storage_blob_core.c"\n',
              '    "storage_blob_core.c"\n    "storage_blob_recovery.c"\n',
          )

          replace_once(
              "firmware/components/web_server/include/web_diagnostics.h",
              "    bool invalid_names_truncated;\n    char invalid_names[WEB_DIAGNOSTICS_INVALID_NAME_MAX][WEB_DIAGNOSTICS_INVALID_NAME_CAPACITY];\n",
              "    bool invalid_names_truncated;\n"
              "    char invalid_names[WEB_DIAGNOSTICS_INVALID_NAME_MAX][WEB_DIAGNOSTICS_INVALID_NAME_CAPACITY];\n"
              "    size_t temporary_file_count;\n"
              "    size_t reported_temporary_file_count;\n"
              "    bool temporary_files_truncated;\n"
              "    char temporary_files[WEB_DIAGNOSTICS_INVALID_NAME_MAX]\n"
              "                        [WEB_DIAGNOSTICS_INVALID_NAME_CAPACITY];\n",
          )

          replace_once(
              "firmware/components/web_server/web_server_diagnostics.c",
              "    if (storage_diagnostics->invalid_names_truncated ||\n        storage_diagnostics->reported_invalid_name_count !=\n            storage_diagnostics->summary.invalid_name_count ||\n        storage_diagnostics->reported_invalid_name_count > WEB_DIAGNOSTICS_INVALID_NAME_MAX) {\n",
              "    if (storage_diagnostics->invalid_names_truncated ||\n"
              "        storage_diagnostics->reported_invalid_name_count !=\n"
              "            storage_diagnostics->summary.invalid_name_count ||\n"
              "        storage_diagnostics->reported_invalid_name_count > WEB_DIAGNOSTICS_INVALID_NAME_MAX ||\n"
              "        storage_diagnostics->temporary_files_truncated ||\n"
              "        storage_diagnostics->reported_temporary_file_count !=\n"
              "            storage_diagnostics->summary.temporary_file_count ||\n"
              "        storage_diagnostics->reported_temporary_file_count >\n"
              "            WEB_DIAGNOSTICS_INVALID_NAME_MAX) {\n",
          )
          replace_once(
              "firmware/components/web_server/web_server_diagnostics.c",
              "    for (size_t index = 0U; index < storage_diagnostics->reported_invalid_name_count; ++index) {\n        const size_t length = strlen(storage_diagnostics->invalid_names[index]);\n        if (length >= WEB_DIAGNOSTICS_INVALID_NAME_CAPACITY) {\n            return APP_ERROR_STORAGE_CORRUPT;\n        }\n        memcpy(out_blob_scan->invalid_names[index], storage_diagnostics->invalid_names[index],\n               length + 1U);\n    }\n    return APP_ERROR_NONE;\n",
              "    for (size_t index = 0U; index < storage_diagnostics->reported_invalid_name_count; ++index) {\n"
              "        const size_t length = strlen(storage_diagnostics->invalid_names[index]);\n"
              "        if (length >= WEB_DIAGNOSTICS_INVALID_NAME_CAPACITY) {\n"
              "            return APP_ERROR_STORAGE_CORRUPT;\n"
              "        }\n"
              "        memcpy(out_blob_scan->invalid_names[index], storage_diagnostics->invalid_names[index],\n"
              "               length + 1U);\n"
              "    }\n"
              "    out_blob_scan->temporary_file_count =\n"
              "        storage_diagnostics->summary.temporary_file_count;\n"
              "    out_blob_scan->reported_temporary_file_count =\n"
              "        storage_diagnostics->reported_temporary_file_count;\n"
              "    for (size_t index = 0U; index < storage_diagnostics->reported_temporary_file_count;\n"
              "         ++index) {\n"
              "        const size_t length = strlen(storage_diagnostics->temporary_files[index]);\n"
              "        if (length >= WEB_DIAGNOSTICS_INVALID_NAME_CAPACITY) {\n"
              "            return APP_ERROR_STORAGE_CORRUPT;\n"
              "        }\n"
              "        memcpy(out_blob_scan->temporary_files[index],\n"
              "               storage_diagnostics->temporary_files[index], length + 1U);\n"
              "    }\n"
              "    return APP_ERROR_NONE;\n",
          )

          replace_once(
              "firmware/components/web_server/web_server_adapter_json.c",
              "    writer_append_text(writer, \"],}\");\n",
              "    writer_append_text(writer, \"],}\");\n",
          ) if False else None
          replace_once(
              "firmware/components/web_server/web_server_adapter_json.c",
              "    writer_append_text(writer, \"}]}\");\n",
              "    writer_append_text(writer, \"}]}\");\n",
          ) if False else None
          replace_once(
              "firmware/components/web_server/web_server_adapter_json.c",
              "    writer_append_text(writer, \"]}\");\n}\n\napp_error_code_t web_adapter_build_diagnostics_json",
              "    writer_append_text(writer, \"],\\\"temporaryFileCount\\\":\");\n"
              "    append_uint64(writer, (uint64_t)diagnostics->temporary_file_count);\n"
              "    writer_append_text(writer, \",\\\"temporaryFiles\\\":[\");\n"
              "    for (size_t index = 0U; index < diagnostics->reported_temporary_file_count;\n"
              "         ++index) {\n"
              "        if (index > 0U) {\n"
              "            writer_append_text(writer, \",\");\n"
              "        }\n"
              "        writer_append_text(writer, \"\\\"\");\n"
              "        writer_append_escaped(writer, diagnostics->temporary_files[index]);\n"
              "        writer_append_text(writer, \"\\\"\");\n"
              "    }\n"
              "    writer_append_text(writer, \"]}\");\n"
              "}\n\napp_error_code_t web_adapter_build_diagnostics_json",
          )
          replace_once(
              "firmware/components/web_server/web_server_adapter_json.c",
              "    if (snapshot->blob_scan.invalid_names_truncated ||\n        snapshot->blob_scan.reported_invalid_name_count != snapshot->blob_scan.invalid_name_count) {\n",
              "    if (snapshot->blob_scan.invalid_names_truncated ||\n"
              "        snapshot->blob_scan.reported_invalid_name_count !=\n"
              "            snapshot->blob_scan.invalid_name_count ||\n"
              "        snapshot->blob_scan.temporary_files_truncated ||\n"
              "        snapshot->blob_scan.reported_temporary_file_count !=\n"
              "            snapshot->blob_scan.temporary_file_count) {\n",
          )

          replace_once(
              "tests/host/CMakeLists.txt",
              "    ../../firmware/components/storage/storage_blob_core.c\n    ../../firmware/components/storage/storage_blob.c\n    ../../firmware/components/storage/storage_mount_topology.c\n",
              "    ../../firmware/components/storage/storage_blob_core.c\n"
              "    ../../firmware/components/storage/storage_blob_recovery.c\n"
              "    ../../firmware/components/storage/storage_blob.c\n"
              "    ../../firmware/components/storage/storage_mount_topology.c\n",
          )
          replace_once(
              "tests/host/CMakeLists.txt",
              "    ../../firmware/components/storage/storage_fs_ops.c\n    ../../firmware/components/storage/storage_blob_core.c\n    ../../firmware/components/storage/storage_blob.c\n)\n",
              "    ../../firmware/components/storage/storage_fs_ops.c\n"
              "    ../../firmware/components/storage/storage_blob_core.c\n"
              "    ../../firmware/components/storage/storage_blob_recovery.c\n"
              "    ../../firmware/components/storage/storage_blob.c\n"
              ")\n",
          )
          replace_once(
              "tests/host/CMakeLists.txt",
              "register_host_test(storage_mount_tests storage_mount storage)\n",
              "register_host_test(storage_mount_tests storage_mount storage)\n\n"
              "add_test(\n"
              "    NAME storage_no_format_policy\n"
              "    COMMAND\n"
              "        ${CMAKE_COMMAND}\n"
              "        -DSTORAGE_MOUNT_SOURCE=${CMAKE_SOURCE_DIR}/../../firmware/components/storage/storage_mount.c\n"
              "        -P ${CMAKE_SOURCE_DIR}/cmake/storage_no_format_policy.cmake\n"
              ")\n"
              "set_tests_properties(storage_no_format_policy PROPERTIES LABELS \"storage\")\n",
          )

          Path("tests/host/cmake/storage_no_format_policy.cmake").write_text(r'''if(NOT DEFINED STORAGE_MOUNT_SOURCE OR NOT EXISTS "${STORAGE_MOUNT_SOURCE}")
    message(FATAL_ERROR "storage mount source is unavailable")
endif()

file(READ "${STORAGE_MOUNT_SOURCE}" source)
string(REGEX MATCHALL "format_if_mount_failed[ \t\r\n]*=[ \t\r\n]*false" matches "${source}")
list(LENGTH matches match_count)
if(NOT match_count EQUAL 1)
    message(FATAL_ERROR "storage mount must set format_if_mount_failed=false exactly once")
endif()
if(source MATCHES "format_if_mount_failed[ \t\r\n]*=[ \t\r\n]*true|esp_littlefs_format")
    message(FATAL_ERROR "automatic LittleFS formatting is forbidden")
endif()
''')

          replace_once(
              "tests/host/test_storage_mount.c",
              "    app_error_code_t prepare_result;\n",
              "    app_error_code_t mount_web_result;\n"
              "    app_error_code_t mount_data_result;\n"
              "    app_error_code_t prepare_result;\n",
          )
          replace_once(
              "tests/host/test_storage_mount.c",
              "    ++((fixture_t *)context)->mount_web;\n    return APP_ERROR_NONE;\n",
              "    fixture_t *fixture = context;\n"
              "    ++fixture->mount_web;\n"
              "    return fixture->mount_web_result;\n",
          )
          replace_once(
              "tests/host/test_storage_mount.c",
              "    ++((fixture_t *)context)->mount_data;\n    return APP_ERROR_NONE;\n",
              "    fixture_t *fixture = context;\n"
              "    ++fixture->mount_data;\n"
              "    return fixture->mount_data_result;\n",
          )
          replace_once(
              "tests/host/test_storage_mount.c",
              "static void test_prepare_failure_rolls_back(void) {\n",
              "static void test_mount_failures_are_explicit_and_never_prepare(void) {\n"
              "    fixture_t fixture = {.mount_web_result = APP_ERROR_STORAGE_UNAVAILABLE};\n"
              "    storage_mount_state_t state = {0};\n"
              "    storage_mount_ops_t ops = operations(&fixture);\n"
              "    TEST_CHECK_APP_ERROR(APP_ERROR_STORAGE_UNAVAILABLE,\n"
              "                         storage_mount_core_mount(&ops, &state));\n"
              "    TEST_CHECK_EQ_U64(1U, fixture.mount_web);\n"
              "    TEST_CHECK_EQ_U64(0U, fixture.mount_data);\n"
              "    TEST_CHECK_EQ_U64(0U, fixture.prepare);\n"
              "    TEST_CHECK_EQ_U64(0U, fixture.unmount_web);\n"
              "    TEST_CHECK(!state.web_mounted);\n"
              "    TEST_CHECK(!state.data_mounted);\n\n"
              "    fixture = (fixture_t){.mount_data_result = APP_ERROR_STORAGE_UNAVAILABLE};\n"
              "    state = (storage_mount_state_t){0};\n"
              "    ops = operations(&fixture);\n"
              "    TEST_CHECK_APP_ERROR(APP_ERROR_STORAGE_UNAVAILABLE,\n"
              "                         storage_mount_core_mount(&ops, &state));\n"
              "    TEST_CHECK_EQ_U64(1U, fixture.mount_web);\n"
              "    TEST_CHECK_EQ_U64(1U, fixture.mount_data);\n"
              "    TEST_CHECK_EQ_U64(0U, fixture.prepare);\n"
              "    TEST_CHECK_EQ_U64(1U, fixture.unmount_web);\n"
              "    TEST_CHECK(!state.web_mounted);\n"
              "    TEST_CHECK(!state.data_mounted);\n"
              "}\n\n"
              "static void test_prepare_failure_rolls_back(void) {\n",
          )
          replace_once(
              "tests/host/test_storage_mount.c",
              "    test_mount_and_unmount();\n    test_prepare_failure_rolls_back();\n",
              "    test_mount_and_unmount();\n"
              "    test_mount_failures_are_explicit_and_never_prepare();\n"
              "    test_prepare_failure_rolls_back();\n",
          )

          replace_once(
              "tests/host/test_storage_blob.c",
              "    char invalid_names[CAPTURE_MAX][STORAGE_FS_ENTRY_NAME_MAX];\n    size_t invalid_count;\n",
              "    char invalid_names[CAPTURE_MAX][STORAGE_FS_ENTRY_NAME_MAX];\n"
              "    size_t invalid_count;\n"
              "    char temporary_names[CAPTURE_MAX][STORAGE_FS_ENTRY_NAME_MAX];\n"
              "    size_t temporary_count;\n",
          )
          replace_once(
              "tests/host/test_storage_blob.c",
              "static app_error_code_t capture_invalid_name(void *context, const char *name) {\n",
              "static app_error_code_t capture_temporary_name(void *context, const char *name) {\n"
              "    capture_t *capture = context;\n"
              "    if (capture == NULL || name == NULL || capture->temporary_count >= CAPTURE_MAX) {\n"
              "        return APP_ERROR_INTERNAL;\n"
              "    }\n"
              "    const size_t length = strlen(name);\n"
              "    if (length >= sizeof(capture->temporary_names[0])) {\n"
              "        return APP_ERROR_INTERNAL;\n"
              "    }\n"
              "    memcpy(capture->temporary_names[capture->temporary_count], name, length + 1U);\n"
              "    ++capture->temporary_count;\n"
              "    return APP_ERROR_NONE;\n"
              "}\n\n"
              "static app_error_code_t capture_invalid_name(void *context, const char *name) {\n",
          )
          replace_once(
              "tests/host/test_storage_blob.c",
              "    TEST_CHECK(!storage_blob_parse_filename(\"00000000000000000001.gz\", NULL));\n\n",
              "    TEST_CHECK(!storage_blob_parse_filename(\"00000000000000000001.gz\", NULL));\n\n"
              "    TEST_CHECK(storage_blob_parse_temporary_filename(\n"
              "        \"00000000000000000001.gz.tmp\", &id));\n"
              "    TEST_CHECK_EQ_U64(1U, id);\n"
              "    TEST_CHECK(!storage_blob_parse_temporary_filename(\n"
              "        \"00000000000000000000.gz.tmp\", &id));\n"
              "    TEST_CHECK(!storage_blob_parse_temporary_filename(\n"
              "        \"00000000000000000001.gz\", &id));\n"
              "    TEST_CHECK(!storage_blob_parse_temporary_filename(\"notes.tmp\", &id));\n\n",
          )
          replace_once(
              "tests/host/test_storage_blob.c",
              "        .visit_invalid_name = capture_invalid_name,\n",
              "        .visit_invalid_name = capture_invalid_name,\n"
              "        .visit_temporary_file = capture_temporary_name,\n",
          )
          replace_once(
              "tests/host/test_storage_blob.c",
              "    TEST_CHECK_EQ_U64(3U, summary.invalid_name_count);\n",
              "    TEST_CHECK_EQ_U64(2U, summary.invalid_name_count);\n"
              "    TEST_CHECK_EQ_U64(1U, summary.temporary_file_count);\n",
          )
          replace_once(
              "tests/host/test_storage_blob.c",
              "    TEST_CHECK_EQ_U64(3U, capture.invalid_count);\n",
              "    TEST_CHECK_EQ_U64(2U, capture.invalid_count);\n"
              "    TEST_CHECK_EQ_U64(1U, capture.temporary_count);\n"
              "    TEST_CHECK_EQ_STRING(\"00000000000000000004.gz.tmp\",\n"
              "                         capture.temporary_names[0]);\n",
          )

          recovery_tests = r'''
static int fail_unlink(void *context, const char *path) {
    (void)context;
    (void)path;
    return -1;
}

static void test_boot_recovery_removes_only_canonical_regular_temporaries(void) {
    test_temp_dir_t directory = {0};
    test_temp_dir_create(&directory);
    char repository[TEST_TEMP_DIR_PATH_MAX];
    path_join(repository, sizeof(repository), directory.path, "repository");
    TEST_CHECK(mkdir(repository, (mode_t)0700) == 0);

    char final_path[TEST_TEMP_DIR_PATH_MAX];
    path_join(final_path, sizeof(final_path), repository, "00000000000000000001.gz");
    create_file_with_size(final_path, 7U);
    char temporary_path[TEST_TEMP_DIR_PATH_MAX];
    path_join(temporary_path, sizeof(temporary_path), repository,
              "00000000000000000002.gz.tmp");
    create_file_with_size(temporary_path, 3U);
    char arbitrary_tmp[TEST_TEMP_DIR_PATH_MAX];
    path_join(arbitrary_tmp, sizeof(arbitrary_tmp), repository, "notes.tmp");
    create_file_with_size(arbitrary_tmp, 2U);
    char temporary_directory[TEST_TEMP_DIR_PATH_MAX];
    path_join(temporary_directory, sizeof(temporary_directory), repository,
              "00000000000000000003.gz.tmp");
    TEST_CHECK(mkdir(temporary_directory, (mode_t)0700) == 0);

    storage_blob_recovery_summary_t recovery = {0};
    TEST_CHECK_APP_ERROR(APP_ERROR_NONE,
                         storage_blob_recover_with_ops(storage_fs_ops_posix(), repository,
                                                       &recovery));
    TEST_CHECK_EQ_U64(1U, recovery.removed_temporary_file_count);
    TEST_CHECK(access(temporary_path, F_OK) != 0);
    TEST_CHECK(access(final_path, F_OK) == 0);
    TEST_CHECK(access(arbitrary_tmp, F_OK) == 0);
    TEST_CHECK(access(temporary_directory, F_OK) == 0);

    storage_blob_scan_summary_t summary = {0};
    capture_t capture = {0};
    const storage_blob_scan_observer_t observer = {
        .context = &capture,
        .visit_entry = capture_entry,
        .visit_invalid_name = capture_invalid_name,
        .visit_temporary_file = capture_temporary_name,
    };
    TEST_CHECK_APP_ERROR(APP_ERROR_NONE,
                         storage_blob_scan_with_ops(storage_fs_ops_posix(), repository, 0U,
                                                    &observer, &summary));
    TEST_CHECK_EQ_U64(1U, summary.valid_count);
    TEST_CHECK_EQ_U64(2U, summary.invalid_name_count);
    TEST_CHECK_EQ_U64(0U, summary.temporary_file_count);
    TEST_CHECK_EQ_U64(0U, capture.temporary_count);

    test_temp_dir_remove(&directory);
}

static void test_boot_recovery_reports_unlink_failure(void) {
    test_temp_dir_t directory = {0};
    test_temp_dir_create(&directory);
    char repository[TEST_TEMP_DIR_PATH_MAX];
    path_join(repository, sizeof(repository), directory.path, "repository");
    TEST_CHECK(mkdir(repository, (mode_t)0700) == 0);
    char temporary_path[TEST_TEMP_DIR_PATH_MAX];
    path_join(temporary_path, sizeof(temporary_path), repository,
              "00000000000000000002.gz.tmp");
    create_file_with_size(temporary_path, 3U);

    storage_fs_ops_t operations = *storage_fs_ops_posix();
    operations.unlink_path = fail_unlink;
    storage_blob_recovery_summary_t recovery = {0};
    TEST_CHECK_APP_ERROR(APP_ERROR_IO,
                         storage_blob_recover_with_ops(&operations, repository, &recovery));
    TEST_CHECK_EQ_U64(0U, recovery.removed_temporary_file_count);
    TEST_CHECK(access(temporary_path, F_OK) == 0);

    test_temp_dir_remove(&directory);
}

'''
          replace_once(
              "tests/host/test_storage_blob.c",
              "static void test_prepare_rejects_non_directory(void) {\n",
              recovery_tests + "static void test_prepare_rejects_non_directory(void) {\n",
          )
          replace_once(
              "tests/host/test_storage_blob.c",
              "    test_directory_prepare_and_scan();\n    test_prepare_rejects_non_directory();\n",
              "    test_directory_prepare_and_scan();\n"
              "    test_boot_recovery_removes_only_canonical_regular_temporaries();\n"
              "    test_boot_recovery_reports_unlink_failure();\n"
              "    test_prepare_rejects_non_directory();\n",
          )

          replace_once(
              "tests/host/test_web_server_adapter_diagnostics_json.inc",
              "                .invalid_name_count = 2U,\n                .reported_invalid_name_count = 2U,\n                .invalid_names =\n                    {\n                        \"notes.txt\",\n                        \"00000000000000000004.gz.tmp\",\n                    },\n",
              "                .invalid_name_count = 1U,\n"
              "                .reported_invalid_name_count = 1U,\n"
              "                .invalid_names = {\"notes.txt\"},\n"
              "                .temporary_file_count = 1U,\n"
              "                .reported_temporary_file_count = 1U,\n"
              "                .temporary_files = {\"00000000000000000004.gz.tmp\"},\n",
          )
          replace_once(
              "tests/host/test_web_server_adapter_diagnostics_json.inc",
              "        \"\\\"invalidNames\\\":\",     \"\\\"executionState\\\":\",  \"\\\"subsystems\\\":\",\n",
              "        \"\\\"invalidNames\\\":\",     \"\\\"temporaryFileCount\\\":\",\n"
              "        \"\\\"temporaryFiles\\\":\",   \"\\\"executionState\\\":\",\n"
              "        \"\\\"subsystems\\\":\",\n",
          )
          replace_once(
              "tests/host/test_web_server_adapter_diagnostics_json.inc",
              "    TEST_CHECK(strstr(output, \"\\\"blobScan\\\":{\\\"blobCount\\\":3,\\\"invalidNameCount\\\":2\") != NULL);\n    TEST_CHECK(strstr(output, \"\\\"invalidNames\\\":[\\\"notes.txt\\\",\"\n                              \"\\\"00000000000000000004.gz.tmp\\\"]\") != NULL);\n",
              "    TEST_CHECK(strstr(output,\n"
              "                      \"\\\"blobScan\\\":{\\\"blobCount\\\":3,\\\"invalidNameCount\\\":1\") != NULL);\n"
              "    TEST_CHECK(strstr(output, \"\\\"invalidNames\\\":[\\\"notes.txt\\\"]\") != NULL);\n"
              "    TEST_CHECK(strstr(output,\n"
              "                      \"\\\"temporaryFileCount\\\":1,\\\"temporaryFiles\\\":[\"\n"
              "                      \"\\\"00000000000000000004.gz.tmp\\\"]\") != NULL);\n",
          )
          replace_once(
              "tests/host/test_web_server_adapter_diagnostics_json.inc",
              "    snapshot = default_diagnostics_snapshot();\n    snapshot.blob_scan.reported_invalid_name_count = 1U;\n",
              "    snapshot = default_diagnostics_snapshot();\n"
              "    snapshot.blob_scan.reported_invalid_name_count = 0U;\n",
          )
          replace_once(
              "tests/host/test_web_server_adapter_diagnostics_json.inc",
              "    TEST_CHECK_EQ_STRING(\"\", output);\n}\n\nstatic void test_diagnostics_failed_query_never_fabricates_data",
              "    TEST_CHECK_EQ_STRING(\"\", output);\n\n"
              "    snapshot = default_diagnostics_snapshot();\n"
              "    snapshot.blob_scan.temporary_files_truncated = true;\n"
              "    TEST_CHECK_APP_ERROR(APP_ERROR_STORAGE_CORRUPT,\n"
              "                         web_adapter_build_diagnostics_json(&snapshot, output,\n"
              "                                                            sizeof(output)));\n"
              "    TEST_CHECK_EQ_STRING(\"\", output);\n\n"
              "    snapshot = default_diagnostics_snapshot();\n"
              "    snapshot.blob_scan.reported_temporary_file_count = 0U;\n"
              "    TEST_CHECK_APP_ERROR(APP_ERROR_STORAGE_CORRUPT,\n"
              "                         web_adapter_build_diagnostics_json(&snapshot, output,\n"
              "                                                            sizeof(output)));\n"
              "    TEST_CHECK_EQ_STRING(\"\", output);\n"
              "}\n\nstatic void test_diagnostics_failed_query_never_fabricates_data",
          )
          PY

          clang-format -i \
            firmware/components/storage/include/storage_blob.h \
            firmware/components/storage/storage_blob_internal.h \
            firmware/components/storage/storage_blob_recovery.c \
            firmware/components/storage/storage_blob_core.c \
            firmware/components/storage/storage_blob.c \
            firmware/components/storage/storage_mount_topology.c \
            firmware/components/web_server/include/web_diagnostics.h \
            firmware/components/web_server/web_server_diagnostics.c \
            firmware/components/web_server/web_server_adapter_json.c \
            tests/host/test_storage_mount.c \
            tests/host/test_storage_blob.c \
            tests/host/test_web_server_adapter_diagnostics_json.inc

      - name: Run host validation
        run: |
          set -o pipefail
          ./scripts/run-tests.sh

      - name: Verify scope and self-delete
        shell: bash
        run: |
          set -euo pipefail
          rm .github/workflows/v2-033-implement.yml
          git diff --check
          git status --short

      - name: Commit implementation
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add --all
          git commit -m "Implement V2-033 boot recovery and diagnostics"
          git push origin HEAD:master
