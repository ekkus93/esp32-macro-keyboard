# ESP32 Macro Keyboard — Retired v1 Specification

**Document status:** Retired. Non-normative.

The authoritative implementation specification is:

- [`docs/SPEC_V2.md`](SPEC_V2.md)

Do not implement, review, test, or make product decisions from the former v1
specification. It described the retired firmware-owned package and macro data
model and conflicts with the v2 rebuild architecture.

The complete former v1 document remains available in git history for historical
investigation only.
