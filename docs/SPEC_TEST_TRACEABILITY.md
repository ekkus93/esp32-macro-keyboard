# Retired specification traceability report

This path is retained only to prevent old links from failing.

The authoritative v2 traceability worklist is
[`SPEC_V2_TEST_TRACEABILITY.md`](SPEC_V2_TEST_TRACEABILITY.md). It is generated
from `docs/SPEC_V2.md` and `docs/UI_UX_SPEC_V2.md` by
`python3 scripts/generate-spec-traceability.py`.

`docs/SPEC.md` is retired and is not an implementation authority for the v2
rebuild.
